#include<stdio.h>
int main()
{
    int a,sum;
    scanf("%d",&a);
    if((a>=10000)&&(a<=99999))
    {
        for(int i=1;i<=4;i++)
        {
            int b=a/10;
            int c=a%10;
            sum=sum+c;
            a=b;
        }
        
        printf("%d",sum);
    }
    return 0;
}
