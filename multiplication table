#include<stdio.h>
int main()
{
    int a;int c;
    printf("enter which table to be displayed:");
    scanf("%d",&a);
    for(int i=1;i<=10;i++)
    {
        c=a*i;
        printf("%dx%d=%d \n",a,i,c);
    }
    return 0;
}
